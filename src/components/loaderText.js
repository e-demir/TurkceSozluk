import React from "react"
import Box from "./box"

export default function LoaderText({ children, border, ...props }) {
    return (
        <Box width={120} bg="light" height={16} {...props}/>
            
        
    )
}

